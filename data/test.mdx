---
title: test
publishedOn: January, 7th. 2022
tags: test
excerpt: Lorem, ipsum dolor sit amet consectetur adipisicing elit. Ea deserunt ab maiores eligendi nemo, ipsa, pariatur
---

import { HeroContainer } from "../components/Hero";

<HeroContainer></HeroContainer>

# test

```typescript {1-3,4}
type GenericHasX<T, K extends keyof T> = T[K] extends undefined ? {} : { K: T[K] };

type BaseType = {
  a?: string;
};

type ABC = {
  a: string;
};

type DEF = {
  b: string;
};

type X = GenericHasX<ABC, "a">;
```
